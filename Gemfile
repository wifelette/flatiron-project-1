source 'https://rubygems.org'

gem 'pry-byebug'
gemspec
# There was a bugfix I needed that isn't in the published gem yet, but has been merged. 
# This overwrites the standard gem for now, so I can use the newest code.
gem 'tty-prompt', git: "https://github.com/piotrmurach/tty-prompt.git"